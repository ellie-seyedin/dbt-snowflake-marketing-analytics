WITH ads AS (
    SELECT * FROM {{ ref('stg_ads') }}
)
SELECT
    *,
    CASE WHEN IMPRESSIONS > 0 THEN CLICKS / IMPRESSIONS ELSE NULL END AS ctr,
    CASE WHEN CLICKS > 0 THEN SPENT / CLICKS ELSE NULL END AS cpc
FROM ads